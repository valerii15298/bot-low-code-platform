{
  "name": "bot-low-code-platform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "startAll": "pnpm -C packages/web run codegen & pnpm -C packages/api exec prisma migrate deploy && pnpm -C packages/api exec prisma generate && pnpm -r dev",
    "startWeb": "pnpm -C packages/web run dev",
    "startTest": "echo 2",
    "docker:clean": "docker rmi $(docker images -f \"dangling=true\" -q)"
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
